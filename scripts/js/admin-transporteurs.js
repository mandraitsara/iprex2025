/** ------------------------------------------------------------------------ JS - Transporteurs Copyright (C) 2020 Intersed http://www.intersed.fr/ ------------------------------------------------------------------------ @author    Cédric Bouillon @copyright Copyright (c) 2020 Intersed @version   1.0 @since     2020 ------------------------------------------------------------------------ */$(document).ready(function() {    "use strict";    // Affiche la liste des transporteurs (aJax)    chargeListeTransporteurs();    // Export PDF    $('.btnExportPdf').click(function() {        var objetDomBtnExport = $(this);        objetDomBtnExport.find('i').removeClass('fa-file-pdf').addClass('fa-spin fa-spinner');        objetDomBtnExport.attr('disabled', 'disabled');        $.fn.ajax({            'script_execute': 'fct_transporteurs.php',            'arguments': 'mode=exportPdf',            'callBack' : function (url_fichier) {                objetDomBtnExport.find('i').removeClass('fa-spin fa-spinner').addClass('fa-file-pdf');                objetDomBtnExport.prop('disabled', false);                $('#lienPdf').attr('href', url_fichier);                $('#lienPdf')[0].click();            } // FIN callBack        }); // FIN ajax    }); // FIN bouton PDF    // Chargement du contenu de la modale Transporteur à son ouverture    $('#modalFournisseur').on('show.bs.modal', function (e) {        // On récupère l'ID de l'utilisateur.        var transporteur_id = e.relatedTarget.attributes['data-transporteur-id'] === undefined ? 0 : parseInt(e.relatedTarget.attributes['data-transporteur-id'].value);        // On récupère le contenu de la modale        $.fn.ajax({            'script_execute': 'fct_transporteurs.php',            'arguments': 'mode=modalTransporteur&id=' + transporteur_id,            'callBack': function (retour) {                // On intègre les différents contenus                var retours = retour.toString().split('^');                var titre = retours[0];                var body = retours[1];                var footer = retours[2];                $('#modalFournisseurTitre').html(titre);                $('#modalFournisseurBody').html(body);                $('#modalFournisseurFooter').html(footer);                // Initialisation des outils JS                $('.selectpicker').selectpicker('render');                $('.togglemaster').bootstrapToggle();                $('[data-toggle="tooltip"]').tooltip();                // Appel au listener de la modale                transporteurModalListener();            } // FIN Callback        }); // FIN aJax    }); // Fin chargement du contenu de la modale    // Nettoyage du contenu de la modale Détails à sa fermeture    $('#modalFournisseur').on('hidden.bs.modal', function (e) {        $('#modalFournisseurTitre').html('<i class="fa fa-spin fa-spinner fa-2x"></i>');        $('#modalFournisseurBody').html('<i class="fa fa-spin fa-spinner fa-2x"></i>');        $('#modalFournisseurFooter').html('<i class="fa fa-spin fa-spinner fa-2x"></i>');    }); // FIN fermeture modale Détails    // Recherche    $('.btnRecherche').click(function() {        $.fn.ajax({            'script_execute': 'fct_transporteurs.php',            'form_id': 'filtres',            'return_id': 'listeTransporteurs',            'callBack': function (retour) {                listeTransporteursListener();            } // FIN Callback        }); // FIN ajax    }); // FIN bouton recherche    $('form#filtres input[type=text]').keyup(function (e) {        var code = (e.keyCode ? e.keyCode : e.which);        if (code === 13) {            e.preventDefault();            $('.btnRecherche').trigger('click');            return false;        }    });    $('form#filtres').submit(function() {        return false;    });    // Nettoyage du contenu de la modale Adresses à sa fermeture    $('#modalAdresses').on('hidden.bs.modal', function (e) {        $('#modalAdressesTitre').html('<i class="fa fa-spin fa-spinner fa-2x"></i>');        $('#modalAdressesBody').html('<i class="fa fa-spin fa-spinner fa-2x"></i>');    }); // FIN fermeture modale Adresses    // Nettoyage du contenu de la modale Contacts à sa fermeture    $('#modalContacts').on('hidden.bs.modal', function (e) {        $('#modalContactsTitre').html('<i class="fa fa-spin fa-spinner fa-2x"></i>');        $('#modalContactsBody').html('<i class="fa fa-spin fa-spinner fa-2x"></i>');    }); // FIN fermeture modale Contacts    // Chargement du contenu de la modale Adresses à son ouverture    $('#modalAdresses').on('show.bs.modal', function (e) {        // On récupère l'ID de l'utilisateur.        var transporteur_id = e.relatedTarget.attributes['data-transporteur-id'] === undefined ? 0 : parseInt(e.relatedTarget.attributes['data-transporteur-id'].value);        chargeModaleAdresse(transporteur_id);    }); // Fin chargement du contenu de la modale    // Chargement du contenu de la modale Contacts à son ouverture    $('#modalContacts').on('show.bs.modal', function (e) {        // On récupère l'ID de l'utilisateur.        var transporteur_id = e.relatedTarget.attributes['data-transporteur-id'] === undefined ? 0 : parseInt(e.relatedTarget.attributes['data-transporteur-id'].value);        chargeModaleContacts(transporteur_id);    }); // Fin chargement du contenu de la modale}); // FIN ready/** ****************************************** * Listener de la modale transporteur ****************************************** */function transporteurModalListener() {    // --------------------------------    // Bouton Enregistrer    // --------------------------------    $('.btnSaveFournisseur').off("click.btnSaveFournisseur").on("click.btnSaveFournisseur", function(e) {        e.preventDefault();                // Initialisation des variables        var erreurs             = false;        var champsObligaroires  = ['nom'];        // On boucle sur tous les champs obligatoires        $.each( champsObligaroires, function( key, value ) {                // Si le champ est vide                if ($('#input_'+value).val().length < 1 ) {                        // On affiche l'invalidité sur le champ correspondant (Bootstrap) pendant 3 secondes                        $('#input_'+value).addClass('is-invalid');                        setTimeout(function(){                            $('#input_'+value).removeClass('is-invalid');                        }, 3000);                    // On déclare l'erreur                    erreurs = true;                } // FIN test validité du champ        }); // FIN boucle sur les champs à vérifier        // Si pas d'erreur jusque là on continue...        if (erreurs === false) {            var nom = $('#formTransporteurAddUpd').find('input[name=nom]').val().trim();            nom = nom.replace('&', '#et#');            $('#formTransporteurAddUpd').find('input[name=nom]').val(nom);            goAddUpd();        } // FIN test pas d'erreur        return false;    }); // FIN bouton Enregistrer    // --------------------------------    // Bouton Supprimer    // --------------------------------    $('.btnSupprimeTransporteur').off("click.btnsupprimetransporteur").on("click.btnsupprimetransporteur", function(e) {        e.preventDefault();        // Si on ne trouve pas l'ID du transporteur, on ne va pas plus loin...        if ( $('#formTransporteurAddUpd').find('input[name=transporteur_id]') === undefined) { return false; }        // On initialise, formate et contrôle l'ID du transporteur        var id_transporteur = parseInt($('#formTransporteurAddUpd').find('input[name=transporteur_id]').val());        if (id_transporteur === 0) { return false; }        // Message de confirmation        var texte = "ATTENTION !\r\nVous allez supprimer un transporteur de l'intranet.\r\nContinuer ?";        if (!confirm(texte)) { return false; }        // Confirmation OK, on supprime le transporteur (aJax)        $.fn.ajax({            'script_execute': 'fct_transporteurs.php',            'arguments': 'mode=supprTransporteur&id_transporteur='+id_transporteur,            'callBack': function () {                // On recharge la liste à jour des transporteurs et on ferme la modale.                chargeListeTransporteurs();                $('#modalFournisseurBody').modal('hide');            } // FIN callback        }); // FIN ajax        return false;    }); // FIN bouton supprime} // FIN listener/** ****************************************** * Affiche la liste des transporteurs ****************************************** */function chargeListeTransporteurs() {    $.fn.ajax({        'script_execute': 'fct_transporteurs.php',        'arguments': 'mode=showlisteTransporteurs',        'return_id': 'listeTransporteurs',        'callBack': function () {            listeTransporteursListener();        } // FIN callback    }); // FIN ajax    return true;} // FIN fonction/** ****************************************** * Valide l'enregistrement ****************************************** */function goAddUpd () {    $.fn.ajax({        'script_execute': 'fct_transporteurs.php',        'form_id': 'formTransporteurAddUpd',        'callBack': function () {            // on recharge la liste à jour et on ferme la modale            chargeListeTransporteurs();            $('#modalFournisseur').modal('hide');        } // FIN callback    }); // FIN ajax    return false;} // FIN fonction/** ****************************************** * Listener de la liste des transporteurs ****************************************** */function listeTransporteursListener() {    // Pagination Ajax    $(document).on('click','#listeTransporteurs .pagination li a',function(){        if ($(this).attr('data-url') === undefined) { return false; }        // on affiche le loading d'attente        $('#listeTransporteurs').html('<i class="fa fa-spin fa-spinner fa-2x"></i>');        // on fait l'appel ajax qui va rafraichir la liste        $.fn.ajax({script_execute:'fct_transporteurs.php'+$(this).attr('data-url'),return_id:'listeTransporteurs'});        // on désactive le lien hypertexte        return false;    }); // FIN pagination ajax} // FIN fonction/** ****************************************** * Fonction déportée : chargement adresses ****************************************** */function chargeModaleAdresse(transporteur_id, id_adresse) {    "use strict";    if (id_adresse === undefined) { id_adresse = 0; }    // On récupère le contenu de la modale    $.fn.ajax({        'script_execute': 'fct_transporteurs.php',        'arguments': 'mode=modalAdresses&id=' + transporteur_id+'&id_adresse='+id_adresse,        'callBack': function (retour) {            // On intègre les différents contenus            var retours = retour.toString().split('^');            var titre = retours[0];            var body = retours[1];            $('#modalAdressesTitre').html(titre);            $('#modalAdressesBody').html(body);            // Initialisation des outils JS            $('.selectpicker').selectpicker('render');            $('.togglemaster').bootstrapToggle();            $('[data-toggle="tooltip"]').tooltip();            // Appel au listener de la modale            adressesModalListener();        } // FIN Callback    }); // FIN aJax} // FIN fonction/** ****************************************** * Fonction déportée : chargement contacts ****************************************** */function chargeModaleContacts(transporteur_id, id_contact) {    "use strict";    if (id_contact === undefined) { id_contact = 0; }    // On récupère le contenu de la modale    $.fn.ajax({        'script_execute': 'fct_transporteurs.php',        'arguments': 'mode=modalContacts&id=' + transporteur_id+'&id_contact='+id_contact,        'callBack': function (retour) {            // On intègre les différents contenus            var retours = retour.toString().split('^');            var titre = retours[0];            var body = retours[1];            $('#modalContactsTitre').html(titre);            $('#modalContactsBody').html(body);            // Appel au listener de la modale            contactsModalListener();        } // FIN Callback    }); // FIN aJax} // FIN fonction/** ****************************************** * Listener de la modale Adresses Transporteur ****************************************** */function adressesModalListener() {    // --------------------------------    // Enregistre une adresse (addupd)    // --------------------------------    $('.btnAddUpdAdresse').off("click.btnAddUpdAdresse").on("click.btnAddUpdAdresse", function(e) {        e.preventDefault();        var adresse = $('#addUpdAdresseTransporteur input[name=adresse_1]').val();        var id_transporteur = $('#addUpdAdresseTransporteur input[name=id_transporteur]').val();        if (adresse === '') { alert("La première ligne d'adresse ne peut pas être vide !"); return false; }        $(this).html('<i class="fa fa-spin fa-spinner"></i>')        $.fn.ajax({            'script_execute': 'fct_transporteurs.php',            'form_id': 'addUpdAdresseTransporteur',            'callBack': function (retour) {                retour+= '';                if (parseInt(retour) !== 1) {                    alert('ERREUR !\r\n\Echec lors de l\'enregistrement...\r\nCode erreur : ' + retour);                    return false;                }                chargeModaleAdresse(id_transporteur);            } // FIN callback        }); // FIN ajax        return false;    }); // FIN save adresse    // --------------------------------    // Supprime une adresse    // --------------------------------    $('.btnSupprAdresse').off("click.btnSupprAdresse").on("click.btnSupprAdresse", function(e) {        e.preventDefault();        if (!confirm("Supprimer cette adresse ?")) { return false; }        var id_adresse = parseInt($(this).parents('tr').data('id'));        var id_transporteur = parseInt($(this).parents('tr').data('id-transporteur'));        if (id_adresse === 0 || id_adresse === undefined) {            alert('ERREUR !\r\bIdentification de l\'adresse impossible...\r\nCode erreur : PNGAVLJ7');            return false;        }        $(this).find('i.fa').removeClass('fa-trash-alt').addClass('fa-spin fa-spinner');        $.fn.ajax({            'script_execute': 'fct_transporteurs.php',            'arguments': 'mode=supprAdresse&id_adresse=' + id_adresse,            'callBack': function (retour) {                retour+= '';                if (parseInt(retour) !== 1) {                    alert('ERREUR !\r\n\Echec lors de la suppression...\r\nCode erreur : ' + retour);                    return false;                }                chargeModaleAdresse(id_transporteur);            } // FIN Callback        }); // FIN aJax        return false;    }); // FIN supprime adresse    // --------------------------------    // Editer une adresse    // --------------------------------    $('.btnEditAdresse').off("click.btnEditAdresse").on("click.btnEditAdresse", function(e) {        e.preventDefault();        var id_adresse = parseInt($(this).parents('tr').data('id'));        var id_transporteur = parseInt($(this).parents('tr').data('id-transporteur'));        chargeModaleAdresse(id_transporteur, id_adresse);        return false;    }); // FIN éditer adresse} // FIN fonction/** ****************************************** * Listener de la modale Contacts Transporteur ****************************************** */function contactsModalListener() {    // --------------------------------    // Enregistre un contact (addupd)    // --------------------------------    $('.btnAddUpdContact').off("click.btnAddUpdContact").on("click.btnAddUpdContact", function(e) {        e.preventDefault();        var nom = $('#addUpdContactTransporteur input[name=nom]').val();        var id_transporteur = $('#addUpdContactTransporteur input[name=id_transporteur]').val();        if (nom === '') { alert("Le nom du contact ne peut pas être vide !"); return false; }        $(this).html('<i class="fa fa-spin fa-spinner"></i>');        $.fn.ajax({            'script_execute': 'fct_transporteurs.php',            'form_id': 'addUpdContactTransporteur',            'callBack': function (retour) {                retour+= '';                if (parseInt(retour) !== 1) {                    alert('ERREUR !\r\n\Echec lors de l\'enregistrement...\r\nCode erreur : ' + retour);                    return false;                }                chargeModaleContacts(id_transporteur);            } // FIN callback        }); // FIN ajax        return false;    }); // FIN save contact    // --------------------------------    // Supprime un contact    // --------------------------------    $('.btnSupprContact').off("click.btnSupprContact").on("click.btnSupprContact", function(e) {        e.preventDefault();        if (!confirm("Supprimer ce contact ?")) { return false; }        var id_contact = parseInt($(this).parents('tr').data('id'));        var id_transporteur = parseInt($(this).parents('tr').data('id-transporteur'));        if (id_contact === 0 || id_contact === undefined) {            alert('ERREUR !\r\bIdentification du contact impossible...\r\nCode erreur : X01PKKZ8');            return false;        }        $(this).find('i.fa').removeClass('fa-trash-alt').addClass('fa-spin fa-spinner');        $.fn.ajax({            'script_execute': 'fct_transporteurs.php',            'arguments': 'mode=supprContact&id_contact=' + id_contact,            'callBack': function (retour) {                retour+= '';                if (parseInt(retour) !== 1) {                    alert('ERREUR !\r\n\Echec lors de la suppression...\r\nCode erreur : ' + retour);                    return false;                }                chargeModaleContacts(id_transporteur);            } // FIN Callback        }); // FIN aJax        return false;    }); // FIN supprime adresse    // --------------------------------    // Editer un contact    // --------------------------------    $('.btnEditContact').off("click.btnEditContact").on("click.btnEditContact", function(e) {        e.preventDefault();        var id_contact = parseInt($(this).parents('tr').data('id'));        var id_transporteur = parseInt($(this).parents('tr').data('id-transporteur'));        chargeModaleContacts(id_transporteur, id_contact);        return false;    }); // FIN éditer adresse} // FIN fonction