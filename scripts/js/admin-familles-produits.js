/** ------------------------------------------------------------------------ JS - Familles produits Copyright (C) 2018 Intersed http://www.intersed.fr/ ------------------------------------------------------------------------ @author    Cédric Bouillon @copyright Copyright (c) 2018 Intersed @version   1.0 @since     2018 ------------------------------------------------------------------------ */$(document).ready(function() {    "use strict";    // Affiche la liste des produits (aJax)    chargeListeFamillesProduits();    // Chargement du contenu de la modale Famille de Produits à son ouverture    $('#modalProduitEspece').on('show.bs.modal', function (e) {        // On récupère l'ID        var famille_id = e.relatedTarget.attributes['data-famille-id'] === undefined ? 0 : parseInt(e.relatedTarget.attributes['data-famille-id'].value);        // On récupère le contenu de la modale        $.fn.ajax({            'script_execute': 'fct_produits.php',            'arguments': 'mode=modalProduitEspece&id=' + famille_id,            'callBack': function (retour) {                // On intègre les différents contenus                var retours = retour.toString().split('^');                var titre = retours[0];                var body = retours[1];                var footer = retours[2];                $('#modalProduitEspeceTitre').html(titre);                $('#modalProduitEspeceBody').html(body);                $('#modalProduitEspeceFooter').html(footer);                // Initialisation des outils JS                //$('.selectpicker').selectpicker('render');                $('.togglemaster').bootstrapToggle();                $('[data-toggle="tooltip"]').tooltip();                // Appel au listener de la modale                produitEspecesModalListener();            } // FIN Callback        }); // FIN aJax    }); // Fin chargement du contenu de la modale}); // FIN ready/** ****************************************** * Listener de la modale famille produits ****************************************** */function produitEspecesModalListener() {    // --------------------------------    // Bouton Enregistrer    // --------------------------------    $('.btnSaveFamille').off("click.btnsavefamille").on("click.btnsavefamille", function(e) {        e.preventDefault();        // Si le bloc d'alerte de doublon affiché, on le masque (réinitialisation)        if (!$('.doublon').hasClass('d-none')) {            $('.doublon').addClass('d-none')        } // FIN test bloc alerte doublon affiché        // Initialisation des variables        var erreurs             = false;        var champsObligaroires  = ['nom'];        // On boucle sur tous les champs obligatoires        $.each( champsObligaroires, function( key, value ) {                // Si le champ est vide                if ($('#input_'+value).val().length < 1 ) {                    // On affiche l'invalidité sur le champ correspondant (Bootstrap) pendant 3 secondes                    $('#input_'+value).addClass('is-invalid');                    setTimeout(function(){                        $('#input_'+value).removeClass('is-invalid');                    }, 3000);                    // On déclare l'erreur                    erreurs = true;                } // FIN test validité du champ        }); // FIN boucle sur les champs à vérifier        // Si pas d'erreur jusque là on continue...        if (erreurs === false) {            // Vérification qu'il n'existe pas déjà ce nom ou cet agrément (aJax)            var fam_id       = parseInt($('#input_id').val());            var fam_nom      = $('#input_nom').val();            $.fn.ajax({                'script_execute': 'fct_produits.php',                'arguments': 'mode=checkFamilleExisteDeja&id='+fam_id+'&nom='+fam_nom,                'callBack': function (retour) {                    // Si on trouve une concordance déjà enregistrée                    if (parseInt(retour) === 1) {                        // On affiche le message de doublon                        if ($('.doublon').hasClass('d-none')) {                            $('.doublon').removeClass('d-none')                        }                        // Sinon, on peux valider l'enregistrement                    } else {                        goAddUpd();                    } // FIN test retour aJax de nom/numagr déjà attribué                } // FIN callback            }); // FIN aJax        } // FIN test pas d'erreur        return false;    }); // FIN bouton Enregistrer    // --------------------------------    // Bouton Supprimer    // --------------------------------    $('.btnSupprimeFamille').off("click.btnsupprimefamille").on("click.btnsupprimefamille", function(e) {        e.preventDefault();        // Si on ne trouve pas l'ID de la famille de produits, on ne va pas plus loin...        if ( $('#formFamilleAddUpd').find('input[name=famille_id]') === undefined) { return false; }        // On initialise, formate et contrôle l'ID de la famille de produit        var id_famille = parseInt($('#formFamilleAddUpd').find('input[name=famille_id]').val());        if (id_famille === 0) { return false; }        // Message de confirmation        var texte = "ATTENTION !\r\nVous allez supprimer une famille de produits de l'intranet.\r\nLes produits liés seront à reclasser !\r\nContinuer ?";        if (!confirm(texte)) { return false; }        // Confirmation OK, on supprime la famille de produits (aJax)        $.fn.ajax({            'script_execute': 'fct_produits.php',            'arguments': 'mode=supprProduitEspeces&id_famille='+id_famille,            'callBack': function () {                // On recharge la liste à jour des produits et on ferme la modale.                chargeListeFamillesProduits();                $('#modalProduitEspece').modal('hide');            } // FIN callback        }); // FIN ajax        return false;    }); // FIN bouton supprime} // FIN listener/** ****************************************** * Affiche la liste des familles de produits ****************************************** */function chargeListeFamillesProduits() {    $.fn.ajax({        'script_execute': 'fct_produits.php',        'arguments': 'mode=showListeFamillesProduits',        'return_id': 'listeFamillesProduits'    }); // FIN ajax    return true;} // FIN fonction/** ****************************************** * Valide l'enregistrement ****************************************** */function goAddUpd () {    $.fn.ajax({        'script_execute': 'fct_produits.php',        'form_id': 'formFamilleAddUpd',        'callBack': function () {            // on recharge la liste à jour et on ferme la modale            chargeListeFamillesProduits();            $('#modalProduitEspece').modal('hide');        } // FIN callback    }); // FIN ajax    return false;} // FIN fonction