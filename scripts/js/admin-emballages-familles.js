/** ------------------------------------------------------------------------ JS - Familles d'emballages Copyright (C) 2018 Intersed http://www.intersed.fr/ ------------------------------------------------------------------------ @author    Cédric Bouillon @copyright Copyright (c) 2018 Intersed @version   1.0 @since     2018 ------------------------------------------------------------------------ */$(document).ready(function() {    "use strict";    // Affiche la liste des familles d'emballages (aJax)    chargeListeEmballagesFamilles();    // Export PDF    $('.btnExportPdf').click(function() {        var objetDomBtnExport = $(this);        objetDomBtnExport.find('i').removeClass('fa-file-pdf').addClass('fa-spin fa-spinner');        objetDomBtnExport.attr('disabled', 'disabled');        $.fn.ajax({            'script_execute': 'fct_emballages.php',            'arguments': 'mode=exportPdf&type=familles',            'callBack' : function (url_fichier) {                objetDomBtnExport.find('i').removeClass('fa-spin fa-spinner').addClass('fa-file-pdf');                objetDomBtnExport.prop('disabled', false);                $('#lienPdf').attr('href', url_fichier);                $('#lienPdf')[0].click();            } // FIN callBack        }); // FIN ajax    }); // FIN bouton PDF    // Chargement du contenu de la modale Emballage à son ouverture    $('#modalConsommablesFamille').on('show.bs.modal', function (e) {        // On récupère l'ID de l'utilisateur.        var emballage_famille_id = e.relatedTarget.attributes['data-emballage-famille-id'] === undefined ? 0 : parseInt(e.relatedTarget.attributes['data-emballage-famille-id'].value);        // On récupère le contenu de la modale        $.fn.ajax({            'script_execute': 'fct_emballages.php',            'arguments': 'mode=modalConsommablesFamille&id=' + emballage_famille_id,            'callBack': function (retour) {                // On intègre les différents contenus                var retours = retour.toString().split('^');                var titre = retours[0];                var body = retours[1];                var footer = retours[2];                $('#modalConsommablesFamilleTitre').html(titre);                $('#modalConsommablesFamilleBody').html(body);                $('#modalConsommablesFamilleFooter').html(footer);                // Initialisation des outils JS                $('.selectpicker').selectpicker('render');                $('.togglemaster').bootstrapToggle();                $('[data-toggle="tooltip"]').tooltip();                // Appel au listener de la modale                ConsommablesFamilleModalListener();            } // FIN Callback        }); // FIN aJax    }); // Fin chargement du contenu de la modale}); // FIN ready/** ****************************************** * Listener de la modale famille d'emballage ****************************************** */function ConsommablesFamilleModalListener() {    // --------------------------------    // Bouton Enregistrer    // --------------------------------    $('.btnSaveConsommablesFamille').off("click.btnsaveConsommablesFamille").on("click.btnsaveConsommablesFamille", function(e) {        e.preventDefault();        // Si le bloc d'alerte de doublon affiché, on le masque (réinitialisation)        if (!$('.doublon').hasClass('d-none')) {            $('.doublon').addClass('d-none');        } // FIN test bloc alerte doublon affiché        // Initialisation des variables        var erreurs             = false;        var champsObligaroires  = ['nom', 'code'];        // On boucle sur tous les champs obligatoires        $.each( champsObligaroires, function( key, value ) {                // Si le champ est vide                if ($('#input_'+value).val().length < 1 ) {                    // On affiche l'invalidité sur le champ correspondant (Bootstrap) pendant 3 secondes                    $('#input_'+value).addClass('is-invalid');                    setTimeout(function(){                        $('#input_'+value).removeClass('is-invalid');                    }, 3000);                    // On déclare l'erreur                    erreurs = true;                } // FIN test validité du champ        }); // FIN boucle sur les champs à vérifier        // Si pas d'erreur jusque là on continue...        if (erreurs === false) {            // Vérification qu'il n'existe pas déjà ce nom ou cet agrément (aJax)            var emb_id       = parseInt($('#input_id').val());            var emb_code      = $('#input_code').val();            $.fn.ajax({                'script_execute': 'fct_emballages.php',                'arguments': 'mode=checkFamilleExisteDeja&id='+emb_id+'&code='+emb_code,                'callBack': function (retour) {                    // Si on trouve une concordance déjà enregistrée                    if (parseInt(retour) === 1) {                        // On affiche le message de doublon                        if ($('.doublon').hasClass('d-none')) {                            $('.doublon').removeClass('d-none')                        }                        // Sinon, on peux valider l'enregistrement                    } else {                        goAddUpd();                    } // FIN test retour aJax de nom/numagr déjà attribué                } // FIN callback            }); // FIN aJax        } // FIN test pas d'erreur        return false;    }); // FIN bouton Enregistrer    // --------------------------------    // Bouton Supprimer    // --------------------------------    $('.btnSupprimeConsommablesFamille').off("click.btnsupprimeConsommablesFamille").on("click.btnsupprimeConsommablesFamille", function(e) {        e.preventDefault();        // Si on ne trouve pas l'ID de l'emballage, on ne va pas plus loin...        if ( $('#formConsommablesFamilleAddUpd').find('input[name=emballage_famille_id]') === undefined) { return false; }        // On initialise, formate et contrôle l'ID de l'emballage        var id_emballage_famille = parseInt($('#formConsommablesFamilleAddUpd').find('input[name=emballage_famille_id]').val());        if (id_emballage_famille === 0) { return false; }        // Message de confirmation        var texte = "ATTENTION !\r\nVous allez supprimer une famille d'emballage de l'intranet.\r\nContinuer ?";        if (!confirm(texte)) { return false; }        // Confirmation OK, on supprime l'emballage (aJax)        $.fn.ajax({            'script_execute': 'fct_emballages.php',            'arguments': 'mode=supprConsommablesFamille&id_emballage_famille='+id_emballage_famille,            'callBack': function () {                // On recharge la liste à jour des emballages et on ferme la modale.                chargeListeEmballagesFamilles();                $('#modalConsommablesFamille').modal('hide');            } // FIN callback        }); // FIN ajax        return false;    }); // FIN bouton supprime} // FIN listener/** ****************************************** * Affiche la liste des familles d'mballages ****************************************** */function chargeListeEmballagesFamilles() {    "use strict";    var id_type = parseInt($('#id_type').val());    $.fn.ajax({        'script_execute': 'fct_emballages.php',        'arguments': 'mode=showListeEmballagesFamilles&id_type=' + id_type,        'return_id': 'listeEmballagesFamilles'    }); // FIN ajax    return true;} // FIN fonction/** ****************************************** * Valide l'enregistrement ****************************************** */function goAddUpd () {    $.fn.ajax({        'script_execute': 'fct_emballages.php',        'form_id': 'formConsommablesFamilleAddUpd',        'callBack': function () {            // on recharge la liste à jour et on ferme la modale            chargeListeEmballagesFamilles();            $('#modalConsommablesFamille').modal('hide');        } // FIN callback    }); // FIN ajax    return false;} // FIN fonction